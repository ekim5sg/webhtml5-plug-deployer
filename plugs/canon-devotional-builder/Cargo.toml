[package]
name = "canon_devotional_builder"
version = "0.1.0"
edition = "2021"

[dependencies]
yew = { version = "0.21", features = ["csr"] }
web-sys = { version = "0.3", features = [
  "HtmlInputElement",
  "HtmlTextAreaElement",
  "Window",
  "Navigator",
  "Clipboard"
] }