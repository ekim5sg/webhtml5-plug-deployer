name: Build + Deploy webhtml5 plug (Hostek)

on:
  workflow_dispatch:
    inputs:
      plug_name:
        description: "Hostek folder name under https://www.webhtml5.info/ (example: my-new-plug)"
        required: true
        default: "my-new-plug"
      app_dir:
        description: "Path to Trunk project folder (contains index.html + Cargo.toml + styles.css). Example: plugs/my-new-plug"
        required: true
        default: "plugs/my-new-plug"
      clean_remote:
        description: "DANGER: wipe remote folder before upload (true/false)"
        required: true
        default: "false"
  push:
    branches:
      - main

jobs:
  index-only:
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Indexed workflow"

  build-deploy:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Preflight (ensure CSS will be bundled)
        shell: bash
        run: |
          set -euo pipefail
          APP_DIR="${{ github.event.inputs.app_dir }}"
          test -f "$APP_DIR/index.html"
          test -f "$APP_DIR/Cargo.toml"
          test -f "$APP_DIR/styles.css"
          grep -q 'data-trunk rel="css"' "$APP_DIR/index.html"
          grep -q 'href="styles.css"' "$APP_DIR/index.html"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            ${{ github.event.inputs.app_dir }} -> target

      - run: cargo install --locked trunk

      - name: Trunk build
        working-directory: ${{ github.event.inputs.app_dir }}
        shell: bash
        run: trunk build --release --public-url "/${{ github.event.inputs.plug_name }}/"

      - name: Verify dist contains CSS + dist/index.html references it
        shell: bash
        run: |
          set -euo pipefail
          APP_DIR="${{ github.event.inputs.app_dir }}"
          echo "=== dist listing ==="
          ls -la "$APP_DIR/dist"
          echo "=== dist css files ==="
          ls -la "$APP_DIR/dist"/*.css
          echo "=== dist/index.html css references ==="
          grep -n "\.css" "$APP_DIR/dist/index.html"

      - name: Add web.config
        shell: bash
        run: |
          APP_DIR="${{ github.event.inputs.app_dir }}"
          cp "templates/web.config" "$APP_DIR/dist/web.config"

      - uses: actions/upload-artifact@v4
        with:
          name: dist-${{ github.event.inputs.plug_name }}
          path: ${{ github.event.inputs.app_dir }}/dist

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.HOSTEK_FTP_HOST }}
          username: ${{ secrets.HOSTEK_FTP_USER }}
          password: ${{ secrets.HOSTEK_FTP_PASS }}
          server-dir: /${{ github.event.inputs.plug_name }}/
          local-dir: ${{ github.event.inputs.app_dir }}/dist/
          dangerous-clean-slate: ${{ github.event.inputs.clean_remote == 'true' }}
